// This one is a little trick to recreate without rebuilding the whole react library from scratch
// we can borrow this one from react so that we can have a nice demo
import { useState } from 'react'
export default useState